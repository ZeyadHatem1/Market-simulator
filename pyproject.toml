[project]
name = "market-sim"
version = "0.1.0"
description = "Event-driven synthetic financial market simulator."
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "plotly",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

